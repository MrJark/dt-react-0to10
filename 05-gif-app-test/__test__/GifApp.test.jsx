import { render, screen } from '@testing-library/react'
import { GifApp } from '../src/GifApp'

describe('Tarea: hacer las pruebas del componente GifApp que quedan por hacer', () => {


    test('', () => { 
        render( <GifApp/> );
        screen.debug();
    });

});